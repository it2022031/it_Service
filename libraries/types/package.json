{
  "name": "@it-service/common-types",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "type": "module",
  "dependencies": {
    "@types/node": "^22.5.1",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "rimraf lib && tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsc-alias": "^1.8.8"
  }
}
