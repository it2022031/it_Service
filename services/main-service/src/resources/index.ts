import type { Resource } from '@conduitplatform/grpc-sdk';

/**
 * Register Conduit authorization resources here.
 * Example:
 * ```ts
 * import { MyResource } from './MyResource.js';
 * export const resources: Resource[] = [MyResource];
 * ```
 */
export const resources: Resource[] = [];
